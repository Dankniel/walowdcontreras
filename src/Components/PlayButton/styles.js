import {StyleSheet, Dimensions} from 'react-native';

export const styles = StyleSheet.create({
  buttonContainer: {alignSelf: 'center', margin: 30},
  logo: {
    width: 90,
    height: 90,
  },
});
